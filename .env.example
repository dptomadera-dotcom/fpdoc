# Base de datos (PostgreSQL / Supabase)
DATABASE_URL="postgresql://usuario:contraseña@host:5432/postgres"

# JWT
JWT_SECRET="cambia-esto-por-un-secreto-seguro-de-al-menos-32-caracteres"

# Frontend URL (para CORS en producción)
FRONTEND_URL="http://localhost:3000"

# Puerto de la API (opcional, por defecto 4000)
PORT=4000
